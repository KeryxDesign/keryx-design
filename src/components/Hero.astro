<section id="hero" class="hero">
  <div class="container hero-content">
    <p class="hero-badge">Design Studio</p>
    <h1 class="hero-title">
      Creiamo esperienze<br />
      <span class="accent">digitali uniche</span>
    </h1>
    <p class="hero-description">
      Keryx Design trasforma le tue idee in progetti web moderni,
      performanti e su misura.
    </p>
    <div class="hero-actions">
      <a href="#portfolio" class="btn btn-primary">Vedi i lavori</a>
      <a href="#contatti" class="btn btn-outline">Contattaci</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(
      ellipse at 30% 50%,
      rgba(99, 102, 241, 0.08) 0%,
      transparent 60%
    );
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-badge {
    display: inline-block;
    padding: 0.375rem 1rem;
    border: 1px solid var(--color-border);
    border-radius: 2rem;
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }

  .accent {
    color: var(--color-accent);
  }

  .hero-description {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    max-width: 520px;
    margin-bottom: 2.5rem;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
</style>
